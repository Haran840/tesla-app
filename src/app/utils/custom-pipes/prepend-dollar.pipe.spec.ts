import { PrependDollarPipe } from './prepend-dollar.pipe';

describe('PrependDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new PrependDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
